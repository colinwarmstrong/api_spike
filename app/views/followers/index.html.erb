<h2><%= current_user.name %>'s Followers</h2>
<%= @github_followers.each do |follower| %>
  <div class='follower'>
    <p><%= follower.login %></p>
    <img src="<%= follower.image %>"  alt="profile pic">
  </div>
<% end %>
